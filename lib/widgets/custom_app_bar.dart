import 'package:flutter/material.dart';

class CustomAppBa extends StatelessWidget {
  const CustomAppBa({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
